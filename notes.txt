Lessons learn from setting up on Windows:
1. installing Jenkins no space in path, this to help to install plugins (like ruby-runtime)
2. running windows batch command with multiple lines by putting 'CALL' before each command.
3. selenium-ruby can't create reports folder as selenium-behave, selenium-java.
